'use strict';
/** @module theme */

const api = require('./theme.api');
const controllers = require('./theme.controllers');
const dal = require('./theme.dal');
module.exports = { api, controllers, dal };
